module.exports = function(app) {
    app.get('/login', function(req, res){
		res.render('login');
    });

	
	app.post('/login/contas', function(req, res) {
		var LogIn = req.body;
		var connection = app.controllers.connectionFactory();
		var LogInConect = new app.controllers.login(connection);
		/*
		LogInConect.salva(LogIn, function (err, result) {
			//res.redirect('/automovel');
			console.log(err + 'Automovel Salvo!');
		});
		/ * */
		LogInConect.lista(connection, function(err, resposta) {
			// res.render('estados/lista', {list : resposta});
			res.format( {
				html: function() {
					console.log(req[0]+'\n');
					console.log(res+'\n');
					console.log(resposta[0])
					//res.render('/login_lista', {list: resposta});
				},
				json: function() {
					res.json(resposta);
				}
			});
		});

		connection.end();

	});
}